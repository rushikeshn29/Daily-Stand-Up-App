import {HashLoader, MoonLoader} from "react-spinners"
function LazyLoader() {
  return(
    <MoonLoader
    color="#f70000"
    size={60}
    // speedMultiplier={2}
  />
  )
}

export default LazyLoader
